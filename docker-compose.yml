version: "3.8"

services:
  trading:
    build: .
    container_name: palm_trading
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - ./knowledge_base:/app/knowledge_base
    environment:
      - TZ=Asia/Shanghai
